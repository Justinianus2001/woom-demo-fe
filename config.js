window.CONFIG = {
    // API_BASE: "https://woom-music-api.nomad.id.vn",
    API_BASE: "http://localhost:8000",
    TRACK_NAMES: [],
    // Keep false for faster UX (smaller payload over streaming JSON).
    PREFER_LOSSLESS_FLAC: false,
};
